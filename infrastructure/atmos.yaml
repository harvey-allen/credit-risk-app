base_path: .

components:
  terraform:
    base_path: components/terraform

stacks:
  base_path: stacks
  included_paths:
    - "orgs/**/*"
  name_pattern: "{namespace}-{tenant}-{environment}-{stage}"